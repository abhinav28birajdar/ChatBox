/**
 * Root index — this screen is an entry point only.
 * All navigation/redirection is handled centrally in src/app/_layout.tsx.
 * The loading gate overlay in RootLayoutNav covers this until navigation is ready.
 */
export default function RootIndex() {
    return null;
}

